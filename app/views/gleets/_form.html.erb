<%= form_for @gleet, remote: true, :html => { :id => "new_gleet" } do |f| %>
  <%= f.submit "Post", :class => "btn btn-success" %>
  <div class="form-group">
    <%= f.text_area :content %>
    <%= f.hidden_field :user_id, value: current_user.id %>
  </div>
<% end %>
