<!DOCTYPE html>
<html>
<head>
  <title>Glitter</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="navbar navbar-inverse">
    <%= link_to "Glitter Home", root_path, :class => "navbar-text" %>
    <div class="signin">
      <% if current_user %>
        <%= link_to "Log Out", logout_path,  :class => "navbar-text pull-right"  %>
        <div class="navbar-text pull-right"> Logged in as '<%= current_user.handle %>'</div>
      <% else %>
        <%= link_to "Sign Up", new_user_path, :class => "navbar-text pull-right" %>
        <%= link_to "Log In", new_session_path, :class => "navbar-text pull-right" %>
      <% end %>
    </div>
  </div>

  <div class="container">
    <%= yield %>
  </div>

</body>
</html>
